dependencies:
  idf:
    version: ">=5.1"
  lvgl/lvgl:
    version: "^9.5.0"
    rules:
      - if: "target == esp32p4"
  espressif/esp_lvgl_adapter:
    version: "^0.6.2"
    rules:
      - if: "target == esp32p4"
  espressif/esp_lcd_touch_gt911:
    version: "^1.2.0"
    rules:
      - if: "target == esp32p4"
  espressif/esp_new_jpeg:
    version: "1.*"
  espressif/libpng:
    version: "1.*"
